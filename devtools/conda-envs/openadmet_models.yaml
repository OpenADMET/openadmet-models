name: openadmet_models
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip

  - numpy
  - pandas
  - scikit-learn
  - rich
  - intake
  - joblib
  - zarr >=3.0.0

  - rdkit
  - molfeat
  - datamol
  - mordredcommunity
  - lightgbm
  - seaborn
  - splito
  - umap-learn
  - modal

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - phx-class-registry
    - email-validator

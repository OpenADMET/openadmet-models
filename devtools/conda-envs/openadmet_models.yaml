name: openadmet_models
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  
  - numpy
  - pandas
  - scikit-learn

  - molfeat
  - datamol

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov


name: openadmet_models
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip

  - numpy
  - pandas
  - scikit-learn
  - rich
  - intake
  - joblib

  - rdkit
  - molfeat
  - datamol
  - mordredcommunity
  - lightgbm
  - seaborn

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - phx-class-registry
    - email-validator
